# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_USER=your_db_user_here
DB_PASSWORD=your_db_password_here
DB_NAME=motifia
DB_PORT=3306

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
SESSION_SECRET=your_random_session_secret_here
AUTHORIZED_EMAIL=your_authorized_email_here

# Note: Replace the placeholder values with your actual configuration
# Do not commit the actual .env file containing real values

# Add any additional backend environment variables here
# Note: These are sensitive values, never commit the actual .env file 